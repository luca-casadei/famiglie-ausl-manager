import {Text} from 'react-native'

const Home =({navigation, nomeFamiglia}) => {
    return(
    ///TODO: Non funziona
    ///Text>Prova Home {route.params.nomeFamiglia}</Text>
    <Text>Prova Home {route.params.nomeFamiglia}</Text>
    );
}

export default Home;